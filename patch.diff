*** Begin Patch
*** Update File: .workflow/workflow_MCPClient_2025-09-13.md
@@
-    - mcp.json 解析与校验（单会话/多会话）；按配置创建连接。
+    - mcp.json 解析与校验（单会话/多会话）；按配置创建连接。
+      - 路径优先级：用户显式指定 > 环境变量（暂定 MF_MCP_CONFIG）> 当前工作目录 ./mcp.json。
@@
-- **依赖与前置**：Node ≥ 18；Electron 与现仓库一致；网络可用（HTTP/SSE）；mcp.json 路径（建议：pps/desktop/assets/mcp.json，支持 MF_MCP_CONFIG 环境变量覆盖）。
+- **依赖与前置**：Node ≥ 18；Electron 与现仓库一致；网络可用（HTTP/SSE）；mcp.json 路径策略：用户指定或 MF_MCP_CONFIG 环境变量覆盖；若都未提供，则使用当前工作目录下的 ./mcp.json。
@@
-## Phase 2: 传输与配置
+## Phase 2: 传输与配置
@@
-- [ ] **Task 4: mcp.json 解析与校验（schema/默认值/多会话）**
+- [ ] **Task 4: mcp.json 解析与校验（schema/默认值/多会话；路径优先级：用户 > 环境变量 > CWD）**
*** End Patch

# 统一环境变量示例（请复制为 .env 并填写真实值）

# 基础
NODE_ENV=development

# 模型提供方（统一命名）
# 可选：gemini | openai
AI_PROVIDER=gemini
AI_MODEL=
AI_API_KEY=

# OpenAI 兼容 Base URL（可选）
# 当 AI_PROVIDER=openai 或使用 OpenAI 兼容服务（如自建代理）时，可通过该项覆盖默认地址；
# 优先级：运行参数 baseURL > OPENAI_BASE_URL > AI_BASE_URL；请填写完整 URL（含协议，如 http:// 或 https://）。
# 例如：本地代理（带版本前缀 /v1）：
# OPENAI_BASE_URL=http://127.0.0.1:11434/v1
OPENAI_BASE_URL=
# OpenAI 专属变量（仅当 AI_PROVIDER=openai 时生效，且优先于通用 AI_*）
# OPENAI_API_KEY=sk-xxx
# OPENAI_MODEL=gpt-40-mini
OPENAI_API_KEY=
OPENAI_MODEL=

# Qdrant（本阶段仅占位，不强制必填）
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION=docs

# MCP 服务（占位）
MCP_SERVER_URL=
MCP_API_KEY=
